import type { PublicUserResolvers } from "./../../types.generated.js";
export const PublicUser: PublicUserResolvers = {
	/* Implement PublicUser resolver logic here */
	firstName: () => {
		/* PublicUser.firstName resolver is required because PublicUser.firstName exists but PublicUserMapper.firstName does not */
	},
	gradeYear: () => {
		/* PublicUser.gradeYear resolver is required because PublicUser.gradeYear exists but PublicUserMapper.gradeYear does not */
	},
	id: () => {
		/* PublicUser.id resolver is required because PublicUser.id exists but PublicUserMapper.id does not */
	},
	lastName: () => {
		/* PublicUser.lastName resolver is required because PublicUser.lastName exists but PublicUserMapper.lastName does not */
	},
	username: () => {
		/* PublicUser.username resolver is required because PublicUser.username exists but PublicUserMapper.username does not */
	},
};
